import { GeneralDatePipe } from './general-date.pipe';

describe('GeneralDatePipe', () => {
  it('create an instance', () => {
    const pipe = new GeneralDatePipe();
    expect(pipe).toBeTruthy();
  });
});
